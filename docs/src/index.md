# DDEigen

Domain Decomposition based subspace iteration for the generalized eigenproblem `K u = λ M u`.

```@contents
Pages = ["api.md"]
Depth = 2
```

## Overview

This package provides experimental routines for a domain decomposition enhanced inverse / Rayleigh quotient type iteration. See `inf_step` for the local constrained Rayleigh minimization.
